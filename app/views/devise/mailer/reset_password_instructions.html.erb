<p>Hi <%= @resource.name.presence || @resource.email %>,</p>

<p>You requested a password reset for your Relay account. Click the link below to choose a new password:</p>

<p><%= link_to "Reset my password", edit_password_url(@resource, reset_password_token: @token) %></p>

<p>If you didn't make this request, you can safely ignore this email. The link expires in a few hours.</p>

<p>— The Relay team</p>
